#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numpar, numimp, num;
    printf("Ingrese un numero natural\n");
    fflush(stdin);
    scanf("%d", &num);
    numpar = (2) "or", (4)"or" , (6) "or", (8);
    numimp = (1)"or" , (3)"or", (5)"or", (7)"or", (9);
        if (num = numpar)
            printf("Es un mumero par\n");

       else
            printf("Es un numero impar\n");
    return 0;
}
